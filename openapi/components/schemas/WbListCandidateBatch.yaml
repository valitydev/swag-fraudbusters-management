type: object
properties:
  id:
    type: integer
    format: int64
  source:
    type: string
  size:
    type: integer
    format: int32
  fields:
    type: array
    items:
      $ref: '../schemas/ListName.yaml'
  createdAt:
    type: string
    format: date-time
