type: object
properties:
  source:
    type: string
  size:
    type: integer
    format: int64
  fields:
    type: array
    items:
      $ref: '../schemas/ListName.yaml'
  createdAt:
    type: string
    format: date-time
