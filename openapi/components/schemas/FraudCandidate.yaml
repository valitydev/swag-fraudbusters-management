type: object
properties:
  createdAt:
    type: string
    format: date-time
  source:
    type: string
  list:
    $ref: '../schemas/ListType.yaml'
  value:
    type: string
  type:
    type: string
    enum:
      - CARD_TOKEN
      - EMAIL
      - BIN
      - FINGERPRINT
      - IP
