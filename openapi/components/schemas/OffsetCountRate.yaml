type: object
allOf:
  - type: object
    required:
      - count
      - offset
    properties:
      count:
        description: Общее количество платежей
        type: integer
        format: int64
        minimum: 0
      offset:
        description: Номер интервала в списке
        type: integer
        format: int64
        minimum: 0
